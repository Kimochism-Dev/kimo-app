import { NavBar } from "@/components"
import { Flex } from "@/components/ui"
import React from "react"

const Home = () => {
    return (
        <Flex 
            padding="p-0"
        >
            <NavBar/>
        </Flex>
    )
}

export default Home